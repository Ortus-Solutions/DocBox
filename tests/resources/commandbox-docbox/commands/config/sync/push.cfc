/**
 * Push local config settings to a remote endpoint.
 **/
component {

	/**
	 * Run the config sync push command
	 *
	 * @endpointName  The name of the endpoint to push to
	 * @force         Skip confirmation prompt
	 **/
	function run(
		string endpointName = "",
		boolean force       = false
	){
	}

}
