/**
 * Shows the current CommandBox configuration settings.
 **/
component {

	/**
	 * Run the config show command
	 *
	 * @setting  The dot-delimited name of the setting to show
	 * @verbose  Show all config values, including defaults
	 **/
	function run(
		string setting  = "",
		boolean verbose = false
	){
	}

}
